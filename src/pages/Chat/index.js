import React from 'react';
import { Box, Typography } from '@mui/material';

const Chat = () => {
  return (
    <Box m={2} mt={5}>
      <Typography variant="h4" gutterBottom>
        Markzaa
      </Typography>
    </Box>
  );
};

export default Chat;
